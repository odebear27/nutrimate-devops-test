spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.datasource.url=jdbc:h2:mem:nutrimate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false

# spring.datasource.url=jdbc:postgresql://localhost:5432/nutrimate
# spring.datasource.username=postgres
# spring.datasource.password=<password>
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=create
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# spring.jpa.show-sql=true

# when set to true, out of the box, Hibernate translates the bean validation annotations 
# applied to the entities into the DDL schema metadata.
# spring.jpa.properties.hibernate.validator.apply_to_ddl=true

# Hibernate to perform the validation of the entity against the possible null values, 
# for corresponding field is annotated only with @Column(nullable = false).
# To validate value before Hibernate sends any insert or update SQL queries to the database.
# spring.jpa.properties.hibernate.check_nullability=true

